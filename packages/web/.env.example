## Public API base URL for the Next.js app to reach the Nest.js backend.
## Must be NEXT_PUBLIC_* to be exposed to the browser.
## The API prefix (/api/v1) is controlled by APP_GLOBAL_PREFIX in the root .env.
NEXT_PUBLIC_API_URL=http://localhost:3010/api/v1
