# EPIC 2/3 — Self-Review Findings (RV2-n)

Adversarial self-review of the overnight build. Severity: P1 blocks, P2 should-fix,
P3 polish. "Fixed" = committed + re-verified this run.

| ID    | Sev | Area               | Finding                                                                                                                                                                                                                                                         | Status                                                                                                                                                                                                                                                         |
| ----- | --- | ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RV2-6 | P2  | Role-gate          | Sales rep holds `quote:view` → could toggle `/quotes` to the department Board (read-only, but spec §1.2: "never the board").                                                                                                                                    | **FIXED** `05000b8` — Board + toggle gated to `rfq:assign_pricers`; non-managers pinned to List. Verified live as ghadah (List only, no toggle/columns).                                                                                                       |
| RV2-1 | P2  | Backend gap        | `<AcceptAssignSheet>` UserPicker is **empty for a Department Manager** — it calls `GET /users` which requires `users:view`, a perm managers lack (403). The seam is unusable by a real manager.                                                                 | **FIXED** (DM-15a `GET /departments/:id/members` + QP-3-fix). Picker now lists the section's department members via the ServiceCategory→Department fold; verified live as hassan@ (Dept Manager): picker populated → assign Lead → start-pricing → QUO minted. |
| RV2-2 | P3  | Backend robustness | Duplicate `RfqAssignment (rfqId, departmentId)` returns **500, not 409** — Prisma P2002 is unhandled in `createAssignment`. Surfaced when accepting an RFQ that already had assignments.                                                                        | **FIXED** (DM-15b `42feed7`) — `createAssignment` catches P2002 → `ConflictException` (409); test covers it.                                                                                                                                                   |
| RV2-3 | P3  | Fidelity           | Board SLA timer is wall-clock-hours-minus-weekend-days, **not** true business-hours with the admin holidays config. Over a holiday it can over/under-count.                                                                                                     | Logged. Acceptable approximation; precise calc needs the holidays API.                                                                                                                                                                                         |
| RV2-4 | P2  | Deferred feature   | Scope segmented control (My queue / My dept / All, spec §1.2) **not built** — precise filtering needs `assignee`/`dept` fields on the rfq+quote list serializers (additive read-side, but unverified overnight). Data is already permission-scoped server-side. | Deferred to morning.                                                                                                                                                                                                                                           |
| RV2-5 | P3  | Deferred feature   | List view omits the spec's `# RFQ` + `Stage` columns (§1.3) — the `Quote` serializer carries no linked `rfqNumber`.                                                                                                                                             | Deferred (needs a serializer field).                                                                                                                                                                                                                           |

## Positive checks (no issue found)

- **No removed-endpoint calls / dead imports** — grep for `useAssignCoordinator|startPreparation|submit-for-approval|dispatch|recordOutcome` across `packages/web/src` is clean.
- **React-Query invalidation correctness** — verified live: resolving an ask drops `openAskCount` (list "Needs you" chip falls); accept moves the card Incoming→Pricing; decline removes it from Incoming; send/outcome advance the tracker phase + timeline (broadened `['rfqs']` invalidation).
- **Sales never sees assign/pricing** — the /rfqs tracker renders zero assign/pricing controls by construction; engineer (hashim.ali) is object-scope-denied the tracker entirely.
- **Engineer never sees send/outcome** — `can('quote:send')`/`can('quote:set_outcome')` gate the QuoteActionCard; both false for pricers/managers.
- **Interaction states** — every new screen has loading (skeleton), empty (self-hiding cards / EmptyState / column empties), error (ErrorState/DetailError), and partial (per-row busy) states.
- **i18n** — every new string is in both `en.json` + `ar.json`; numbers/IDs `dir="ltr"`, RTL timeline mirrors, 44px touch targets, mobile cards <1024 / desktop table ≥1024.
